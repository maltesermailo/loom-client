// Placeholder translation unit so loom_proto is a real static library target.
// Replaced/augmented by datagram.cpp et al. starting in step 2.
namespace loom::proto {
const char* module_name() { return "loom-proto"; }
} // namespace loom::proto
